{% extends 'base.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}
     {{parent()}}
     {{form(form)}} 
     
     {# {{dump(posts)}}  #}



    <h2 style="color: blue;"> Publica Aqui</h2>
    <table border="1px" >
        <thead>
        <tr>

            <td>id</td>
            <td>titulo</td>
            <td>comentario</td>
            <td>fecha</td>
        
        </tr>
        </thead>
        <tbody>
        {% for post in posts %}

            <tr>
                <td ><a href="{{ path('postDetalles', {id:post.id}) }}">Ver</a></td> 
                <td>{{post.titulo}}</td> 
                <td>{{post.comentario}}</td> 
                <td>{{post.fecha_publicacion | date}}</td> 
            
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
 